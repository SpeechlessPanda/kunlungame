# 把这份文件复制为 .env.local（已在 .gitignore 中），填入你的 OpenAI 兼容服务凭据。
# pnpm dev 启动后，渲染层加载 runtime-state 时会自动套上这些值，免去每次手填。
# 注意：
#   1) 仅在 dev 模式（app.isPackaged === false）生效；安装包不读 .env。
#   2) 一旦在 UI 里点了「保存」，apiKey 会经 Electron safeStorage 加密落盘到
#      %APPDATA%/kunlungame/runtime-state.json；删 .env 不会清持久化的 key，
#      想完全清空请直接删该文件，或在设置面板把 apiKey 清空再保存。
#   3) 设置了 KUNLUN_OPENAI_API_KEY 时，modelProvider 会自动切到 openai-compatible，
#      除非你显式设置 KUNLUN_MODEL_PROVIDER=local。

KUNLUN_OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxx
KUNLUN_OPENAI_BASE_URL=https://api.openai.com/v1
KUNLUN_OPENAI_MODEL=gpt-4o-mini
KUNLUN_OPENAI_FALLBACK_MODELS=

# OpenRouter 免费模型通常更慢且波动更大；2026-04-28 真实 playthrough 验证中，
# openai/gpt-oss-120b:free 稳定性最好；Qwen / GLM 免费档曾出现短答或空流，不作为默认推荐。
# openai/gpt-oss-120b:free 稳定性最好；Qwen / GLM 免费档曾出现短答或空流，不作为默认推荐。
# KUNLUN_OPENAI_MODEL=openai/gpt-oss-120b:free
# KUNLUN_OPENAI_FALLBACK_MODELS=meta-llama/llama-3.3-70b-instruct:free

# 可选：强制走本地档（即便填了上面三项），用于对比测试。
# KUNLUN_MODEL_PROVIDER=local
